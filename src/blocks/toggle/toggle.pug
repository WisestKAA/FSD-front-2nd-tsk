
mixin toggle({ name, text, isChecked=false }={})
  .toggle
    label.toggle__label
      -
        const imputAttr = {
          type: "checkbox", 
          name: name, 
          checked: isChecked, 
          class: 'toggle__input'
        }
      input&attributes(imputAttr)
      .toggle__text
        .toggle__content=text