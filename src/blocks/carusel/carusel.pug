mixin carusel({ imgLinks=[], }={}) 
  figure.carusel.js-carusel
    .carusel__wrapper.owl-carousel
      each item in imgLinks
        - const {src, alt} = item          
        img(src=src alt=alt).carusel__img