mixin social-icons({ icons=[], isForSmallFooter=false }={})
  figure.social-icons(class= isForSmallFooter && 'social-icons_small')
    each icon in icons
      -
        const { link, img, alt } = icon
        const linkAttr = {
          href: link,
          target: '_blank',
          rel: 'noopener noreferrer',
          class: 'social-icons__ico',
        }
      a&attributes(linkAttr)
        img.social-icons__img&attributes({src: img, alt})
