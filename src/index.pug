include common.blocks/text-field/text-field.pug
include common.blocks/dropdown/dropdown.pug

doctype html
html
    head
    body.body
        
        form(action="post")
            +text_field({_placeholder:'E-mail', _id: 'inp1', isMasked: 'true'})
        +dropdown({_text: "hello"})